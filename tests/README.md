# Test workspace

Current automated tests cover the released cohort schema, patient uniqueness, train/holdout non-overlap, outcome/ID exclusion from features, development-only retrieval, saved experiment artifacts, and the Streamlit case-browser smoke path.

Planned tests cover schema validation, patient-level splitting, leakage prevention, missing modalities, calibration, abstention, reproducibility, and safe output behavior. Use synthetic data only until an approved environment exists.
